name: build-on-pre-release

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    environment: dev

    steps:
      - name: Check Out Repository
        uses: actions/checkout@v3

      - name: Tagging
        run: | 
          echo "Testing"  
 